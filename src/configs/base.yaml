project:
  name: satellite-landcover-benchmark
  seed: 42

dataset:
  name: eurosat
  root: ""

model:
  name: resnet18
  pretrained: true
  num_classes: 10

train:
  epochs: 10
  batch_size: 32
  learning_rate: 0.001
  num_workers: 2

optimizer:
  name: adamw
  weight_decay: 0.0001

scheduler:
  name: cosine

paths:
  dataset_dir: ""
  output_dir: ""
  checkpoint_dir: ""