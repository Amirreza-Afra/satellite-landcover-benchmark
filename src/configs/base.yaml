project:
  name: satellite-landcover-benchmark
  seed: 42

dataset:
  name: eurosat
  root: ""

model:
  name: resnet18
  pretrained: true
  num_classes: 10

dataset:
  name: eurosat
  root: ""

data:
  image_size: 224
  batch_size: 32
  num_workers: 2
  
train:
  epochs: 10
  learning_rate: 0.001

optimizer:
  name: adamw
  weight_decay: 0.0001

scheduler:
  name: cosine

paths:
  dataset_dir: ""
  output_dir: ""
  checkpoint_dir: ""